# checkpoint.md - PerfectBundle

> **⚠️ UPDATE THIS FILE BEFORE EVERY COMMIT**

---

## Current Status

| Metric | Value |
|--------|-------|
| **Overall Progress** | ~80% (M1 + M2 P0 done; production Convex deployed; M5 E2E core + cross-browser + a11y green; **M3 Analytics done** — dashboard + funnel + attribution built via PostHog API, event delivery proven with real data; M4 auth/save/profiles + past-bundle memory + Popular tab + signup event + eBay affiliate tag done; real eBay product photos/links/prices live and sharp) |
| **Current Phase** | M4 accounts & retention (save, profiles, recipient prefill, past-bundle memory, Popular tab, signup event, eBay affiliate tag done; occasion reminders is the only remaining M4 item, blocked on owner's Resend key). M3 Analytics milestone closed out this session. |
| **On Track?** | ✅ Yes |
| **Last Updated** | 2026-07-23 |
| **Last Commit** | see change log |

### Tasks Status

See `docs/tasks.md` for the live task list. Summary:

| Metric | Value |
|--------|-------|
| **Active Milestone** | M4 accounts & retention (save/profiles/past-bundle memory/Popular tab/signup event/eBay affiliate tag done; occasion reminders is the only item left, blocked on Resend key). M3 Analytics closed out this session. |
| **Tasks Complete** | 58 / ~85 |
| **Tasks In Progress** | 0 |
| **Tasks Blocked** | 1 (occasion reminders — needs owner's Resend key) |
| **Active Plan** | `docs/superpowers/plans/2026-07-20-item-swap-bundle-regenerate.md` (complete) |

### Progress by Milestone

| Milestone | Status | Progress | Notes |
|-----------|--------|----------|-------|
| 1. Foundation | ✅ Complete | 100% | Live at perfectbundle.vercel.app; **production Convex deployed** (was local-only, fixed this session); PostHog key + Sentry still deferred (non-blocking) |
| 2. Core MVP (P0) | ✅ Complete | 100% | Quiz, engine, links, results UI, share, trending all done and live-verified. Item swap ("show me another") + per-bundle regenerate shipped 2026-07-20 — see Backlog notes below. Curated bundles grown from 5 to 23 (2026-07-22). |
| 3. Analytics | ✅ Complete | 100% | **Closed out 2026-07-20.** Owner generated a scoped PostHog personal API key (Insight/Dashboard write, Event definition read); used it to (1) prove event delivery with real data instead of guessing — `event_definitions` showed all M2/M3 events with today's `last_seen_at`, and a live funnel query returned genuine conversion numbers (9 `$pageview` → 5 `quiz_started` → 4 `quiz_completed` → 3 `bundles_generated` → 2 `retailer_link_clicked`), finally resolving the "couldn't get automated confirmation" gap from earlier sessions; (2) built "PerfectBundle Proof Dashboard" (pinned) with 12 insights via the PostHog REST API covering every `docs/dashboard-spec.md` section — headline metrics (bundles generated, quiz completion %, buy-intent CTR, shares, all via `TrendsQuery` formulas like `A/B*100`), core funnel (two variants: breakdown by `$device_type` and by `utm_source`), channel attribution (sessions+quiz-starts by UTM, plus a share-loop funnel filtered to `utm_source=share`), 12-week trend line, engine health (cache-hit rate, generation failures), and retention events (ready for M4 events once they start firing). Revenue view stayed manual (P2 by design) and Alerts were skipped (optional polish, would need checking free-tier availability). Note: `docs/dashboard-spec.md`'s "p90 latency" tile was dropped — that property was never actually captured in `bundles_generated`'s event properties (only `cache_hit`/`budget_band` are), so building a filter for it would have been fabricated, not real. **Convex engagementCounters also wired** (`convex/engagement.ts` — clicks/saves/shares/views per bundle, fired from all touchpoints; powers the Popular tab independently of PostHog) |
| — | Real product photos + direct links | ✅ Done (2026-07-19) | **Resolution of the 2026-07-18 strategy pivot.** Etsy's app was rejected and removed entirely from the codebase (link builder + tests). Amazon and **eBay were approved** the same evening, reopening the direct-retailer route — Sovrn (the aggregator fallback) is no longer needed as the primary path. Shipped: (1) representative stock images — **Unsplash primary + Pexels fallback**, env-gated, cached at generation, photographer-credited (Unsplash's terms require it); (2) **eBay Browse API wired as the real-product layer** — OAuth client-credentials token fetched once per generation, per-item search, real photo + direct buyable item link + real price via `chooseItemMedia`'s `realProduct` slot (generalized from the original Sovrn-specific `sovrn` param). **Verified live**: a real (uncached) generation returned genuine eBay photo+price+buy-link for every item across all 3 bundles. Also found and fixed a real pre-existing bug while flipping on Amazon's tag: `retailer-links.ts` read non-`NEXT_PUBLIC_` env vars from a client component, so the affiliate tag never reached the browser bundle — renamed to `NEXT_PUBLIC_AFFILIATE_TAG_AMAZON`/`NEXT_PUBLIC_AFFILIATE_ID_EBAY`, regression-tested. Sovrn env vars/docs remain in place, unused, as a documented future alternate source. |
| 4. Accounts & Retention | 🔄 In Progress | 65% | Clerk auth, save-bundle + guest→signup upsell, and "My bundles" page all live in production. Recipient profiles done: `convex/recipientProfiles.ts` CRUD (auth-gated + ownership-checked), `/profiles` page (create/edit/delete), and a unit-tested prefill helper (`src/lib/quiz/prefill.ts`) for "New bundles for X". Past-bundle memory now live — `recipientProfiles.pastItemNames` feeds an "avoid repeating" prompt instruction, updated after each fresh (non-cached) generation; `profileId` threads through `QuizState` (kept out of `QuizAnswers`/cache hash), and the generation-cache key folds in `profileId` so a stale hit can't bypass dedup. **Popular tab live** — `/popular` ranks publicly-shared user-generated bundles by engagement score (`convex/engagement.ts` `listPopular` + pure `popularityScore`), distinct from editorial `/trending`, cross-linked, with a cold-start empty state. Quiz's hardcoded option lists extracted to `src/lib/quiz/options.ts`. Still using Clerk **dev-mode keys** in production (no prod instance yet). `signup` PostHog event wired via a Clerk `user.created` webhook — **verified fully working end-to-end** (found + fixed a real bug: the signing secret was never pushed to Vercel). eBay Partner Network campaign ID live (`AFFILIATE_ID_EBAY`/`NEXT_PUBLIC_AFFILIATE_ID_EBAY` set on Convex prod + Vercel) — eBay buy-link clicks now earn commission. Google OAuth confirmed already enabled (Clerk's shared dev credentials). Found and disabled Clerk Organizations + org billing on the instance — unused scaffolding default that was forcing an org-selection step into every sign-in. Still to build: occasion reminders (Resend cron, blocked on owner's Resend key) |
| 5. Testing & Polish | 🔄 In Progress | 95% | E2E suite: 71 passing across chromium/firefox/webkit/mobile-chrome + baseline a11y audit passing on all 4. `tests/e2e/auth-flow.spec.ts` covers the full signup → save → profile flow using `@clerk/testing`. **Lighthouse pass done (2026-07-22)**: `/quiz/results` scores 94 perf / 100 a11y; `/quiz` scores 74 perf / 100 a11y — held below 90 by Clerk's dev-instance handshake redirect + its UI bundle size, an architectural cost of decisions already made this session (dev keys, no custom domain), not an app bug. Fixed 2 real a11y bugs found in the process (heading order, color contrast) — see Backlog notes. |
| 6. Launch | ⏳ Not Started | 0% | |

---

## Completed Items ✅

### This Session (2026-07-23)
- [x] **Warm & bold visual redesign — full palette/component pass across the app, verified clean.** Spec: `docs/superpowers/specs/2026-07-23-warm-bold-visual-redesign-design.md`; plan: `docs/superpowers/plans/2026-07-23-warm-bold-visual-redesign.md` (8 tasks, executed via `superpowers:subagent-driven-development`). Replaced the previous black/white theme with a warm palette driven by CSS custom properties (`--background`, `--foreground`, `--bg-alt`, `--fg-muted`, `--accent-from`, `--accent-to`, `--accent-solid`, `--border`, light + dark) and three reusable component classes (`.btn-primary`, `.btn-secondary`, `.card-surface`) added in `src/app/globals.css` alongside a new `src/lib/design/contrast.ts` (`contrastRatio`, `blendHex`) + Vitest suite asserting every token pair clears WCAG AA. Applied across: landing page + header (Task 2), the quiz wizard/chips/urgency step (Task 3), `BundleCard` + `SaveButton` (Task 4 — propagates automatically to every page that renders a bundle card: `/quiz/results`, `/trending`, `/popular`, `/b/[id]`, `/my-bundles`), the quiz-results empty state (Task 5), `/profiles` and `/my-bundles` page-level chrome (Task 6), and Clerk's auth UI (`<SignIn>`/`<SignUp>`/`<UserButton>` themed via `ClerkProvider`'s `appearance.variables`/`elements`, Task 7). Motion/animation was explicitly left untouched per the plan's Global Constraints — palette/typography/surfaces only.
- [x] **Task 8 verification pass — found and fixed two real regressions before this could ship, not just a clean-pass rubber stamp.** Two prior verification attempts (see `.superpowers/sdd/task-8-report.md`) each stopped on a genuine, reproducible bug rather than proceeding on a green-looking suite: (1) **Build-blocking type error** — Task 7's `ClerkProvider` `appearance.variables` object used Clerk appearance-variable key names from an older major version (`colorText`, `colorTextSecondary`, `colorInputBackground`, `colorInputText`); the installed `@clerk/nextjs@7.5.20`'s `Variables` type renamed these to `colorForeground`, `colorMutedForeground`, `colorInput`, `colorInputForeground` — `npx tsc --noEmit`/`next build` failed with `TS2353`, meaning the branch as committed was not deployable. Fixed by renaming the 4 keys (`src/app/layout.tsx`); empirically re-verified via headless `getComputedStyle()` checks on `/sign-in` in both light and dark mode that all 4 renamed keys resolve to the exact same computed colors the original (invalid) names were intended to produce. (2) **WCAG AA color-contrast regression** — a Lighthouse mobile audit against a production build caught `/quiz/results` scoring Accessibility 96 (not 100): 34 flagged elements, all a bare Tailwind `opacity-60` utility on plain `--foreground` text (no explicit color class) rendered against the redesign's new warm backgrounds — 3.7–3.8:1 measured contrast against a 4.5:1 AA requirement. This is the same *class* of bug the 2026-07-22 session fixed once already under the old palette (`opacity-50`→`opacity-60`), but the new palette's absolute colors made `opacity-60` insufficient on its own. Fixed by bumping all 15 real static-text instances (`bundle-card.tsx`, `quiz-wizard.tsx`, `urgency-step.tsx`, `trending`/`profiles`/`popular`/`my-bundles`/`quiz/results` pages) to `opacity-70` (verified ≥4.5:1, measured 4.9–5.1:1 on the real rendered page), added a new TDD'd regression test (`blendHex` + a `contrast.test.ts` block asserting `--foreground` at opacity-70 clears 4.5:1 against both light backgrounds — confirmed RED at the old opacity-60 value first, GREEN at opacity-70) so this exact failure mode can't silently regress again. Both fixes are documented in full, including root-cause analysis and re-verification evidence, in `.superpowers/sdd/task-8-report.md` and `.superpowers/sdd/task-8-fix-contrast-report.md`.
- [x] **Final verification (this pass, attempt 3), all fresh, all genuinely green:** `npm test` — 151/151 passed (149 baseline + 2 new opacity-regression contrast tests). `npx tsc --noEmit` — clean, confirming the Clerk-variable-key fix holds on the current branch tip. `npx playwright test` (full suite, no filter) — 71 passed / 6 skipped, matching the documented baseline exactly, zero new failures. Lighthouse mobile audit against a real production build (`npm run build && npm run start`, via Playwright-driven Chromium + `puppeteer-core.connect` + Lighthouse's Node `navigation()` API, `disableStorageReset: true` so `/quiz/results`' `sessionStorage`-gated content — reached via a real 6-step quiz completion, one genuine Gemini call — is actually visible to the audit): **Accessibility 100 on all 3 pages** (`/`, `/quiz`, `/quiz/results` — zero failing a11y audits in the raw LHR JSON, not just a rounded 100). Performance: `/` 95, `/quiz` 96 (well above the 74 baseline — the Clerk-dev-keys handshake cost documented in §3 is apparently absorbed differently on this worktree/build, not investigated further since it's an improvement, not a regression), `/quiz/results` 94 (matches the 94 baseline exactly, confirmed stable across two independent audit runs of the same page — both scored 94, no repeat of the 83–93 variance the second attempt saw). `puppeteer-core`/`lighthouse` were installed via `npm install --no-save` and fully uninstalled afterward — confirmed via `git status --short package.json package-lock.json` clean before and after, and a `node_modules` check.

### This Session (2026-07-22)
- [x] **Added Google AdSense (owner-provided client ID `ca-pub-9391534437442090`).** Prompted by the same "how do I make money off this" conversation as the OG-image work. Two rounds: (1) site-wide loader script only (`src/app/layout.tsx` via `next/script`, `afterInteractive` strategy — chosen because it doesn't block initial render/LCP) + `public/ads.txt` (required for AdSense to pay full value, not just serve ads). (2) Owner came back with two real ad unit codes (an "auto"/full-width-responsive unit, slot `1834149356`, and a "fluid"/in-feed unit with a specific `data-ad-layout-key`, slot `8812435332`) and pasted raw HTML/script snippets — converted these into a reusable React client component, `src/components/ad-unit.tsx` (`useEffect` + a mount-guard ref pushes `adsbygoogle.push({})` exactly once per instance, avoiding the raw-HTML pattern's per-instance inline `<script>` tags). Asked the owner where to place them (flagged that ads near the affiliate "Buy" buttons could pull clicks away from higher-value affiliate commission) — owner chose **both**: the auto unit as a top banner on `/trending`, and the fluid in-feed unit blended into the bundle list on `/trending`, `/popular`, and `/quiz/results`. Verified all three with a real headless-browser check (`ins.adsbygoogle` locator after `networkidle`) rather than `curl`, since the in-feed units only exist after each page's client-side Convex query resolves — a raw-HTML request only ever shows the top banner (this is expected `"use client"` + `useQuery` behavior, not a bug, and cost time to confirm). Also chased a real "couldn't verify site" error from AdSense after round (1): confirmed via direct HTTP checks against `perfectbundle.vercel.app` that the script is genuinely server-rendered into the raw HTML (not JS-only) and `ads.txt` serves correctly — so the verification failure is external to the code (transient crawler retry, a URL-entry mismatch in the AdSense dashboard, or possibly `vercel.app` shared-subdomain flakiness if retries keep failing, which would reopen the custom-domain question). Verified: Vitest 132/132, `tsc --noEmit` clean, full Playwright suite 71 passed/6 skipped both times (no regression from the new wrapper `<div>`s needed to interleave ad units into each `.map()` list).
- [x] **Added OG/Twitter share preview images.** Prompted by "how do I start marketing this" — the core share loop (`/b/[id]`) had text-only OG meta tags (theme/rationale) but no image, so links shared to Reddit/iMessage/Twitter/X rendered as bare text, which gets much lower click-through than a rich preview card. Added `src/app/opengraph-image.tsx` (site-wide default, used on landing/quiz/trending/etc.) and `src/app/b/[id]/opengraph-image.tsx` (dynamic, fetches the bundle via the same `getPublic` query as the page and renders its theme) using Next's built-in `next/og` `ImageResponse` API — free, no external image-generation service, satisfies the $0 constraint. Also added `twitter: { card: "summary_large_image" }` metadata (root layout + per-bundle `generateMetadata`) so X renders the large-image card instead of a small thumbnail. Verified both images render correctly end-to-end: started a real dev server, hit `/opengraph-image` and `/b/<seeded-id>/opengraph-image` directly, downloaded and visually inspected both PNGs (1200×630, twemoji 🎁 renders in color, text legible) — confirmed via `curl` that the landing page's `<head>` actually emits the `og:image`/`og:image:width`/`og:image:height`/`og:image:alt` tags pointing at the generated route. Vitest 132/132 and `tsc --noEmit` clean.
- [x] **Lighthouse mobile pass on `/quiz` and `/quiz/results`, run against a production build.** Dev-mode (`next dev`) scores are meaningless for this (unminified JS, no code splitting) — rebuilt with `next build && next start` first. `/quiz/results` needs real sessionStorage-backed quiz answers to render its actual content rather than the "No quiz answers found" empty state, which a cold `npx lighthouse <url>` CLI run always hits (it opens a fresh, isolated browsing context) — worked around with a one-off script (`playwright` to drive the real 6-step quiz to completion in a Chromium instance launched with `--remote-debugging-port`, then `puppeteer-core.connect({browserURL})` + Lighthouse's Node `navigation(page, url, {config})` API to audit that *same* already-authenticated tab with `disableStorageReset: true`). `puppeteer-core`/`lighthouse` installed with `--no-save` and uninstalled after — no package.json/lockfile changes. Results: **`/quiz/results` — Performance 94, Accessibility 100** (verified via `network-requests` audit showing real eBay/Unsplash image fetches — this is the actual bundle-loaded page, not the empty state). **`/quiz` — Accessibility 100, Performance 74** (below the ≥90 target). Root-caused the shortfall: the `redirects` audit showed a ~1.7s Clerk "dev browser handshake" round-trip to `*.clerk.accounts.dev` (only happens on a cold/cookie-less visit — exactly what Lighthouse always tests), and `unused-javascript` showed ~230KiB of eagerly-loaded Clerk UI + PostHog session-recorder script. Both are consequences of this session's own earlier decisions (stay on Clerk dev-mode keys, stay on vercel.app rather than buy a custom domain) rather than app-level bugs — a Clerk *production* instance eliminates the handshake entirely but requires DNS control of an owned domain, so not pursued further without revisiting that call. **Found and fixed 2 real, genuine a11y bugs along the way** (both score 0 in axe, both shared across every page that renders `<BundleCard>` — trending, popular, share, quiz results): (1) heading order — the bundle theme was an `<h3>` with no `<h2>` anywhere between it and the page's `<h1>`, on literally every page that uses the component; changed to `<h2>` (`src/components/bundles/bundle-card.tsx`). (2) color contrast — several `text-xs opacity-50` elements (the "at eBay" merchant label, "Representative image · Photo by …" attribution caption + link, and the affiliate disclosure line) computed to `#8b8b8b` on white, a 3.4:1 ratio against the WCAG-required 4.5:1 for small text; bumped to `opacity-60` (~4.67:1, verified via the WCAG relative-luminance formula before touching code). No test assertions pin an explicit heading `level`, so this was a safe, mechanical fix — confirmed via full Playwright suite (71 passed/6 skipped, including the axe-core `accessibility.spec.ts` file) and Vitest (132/132) afterward.
- [x] **Grew curated bundles from 5 to 23** — pre-launch M6 content task (`docs/tasks.md` Backlog item, previously deferred). Hand-authored 18 new bundles in `convex/seedData.ts` (Garden Therapy, Fresh Start Fitness, Level Up Night, Wanderer's Carry-On, Spoiled Rotten (the Dog), The Cat Whisperer's Kit, Paint & Unwind, Vinyl Nights, The Spa Night In, Basecamp Ready, Grandparent Favorites, Teen Approved, Cozy Christmas Morning, Journal & Reflect, Baker's Dozen, Marathon Mindset, Yoga Reset, Board Game Night Host), each with 3 items passing `curatedBundleSchema`, no alcohol/age-restricted items, distinct `sortWeight`. Added `seedAdditionalCurated` (`convex/seed.ts`) — a title-matched, idempotent top-up mutation (the original `seedCurated` only ever runs once ever, so it couldn't be reused for a top-up). Updated `schemas.test.ts` (23-count assertion + a no-duplicate-titles test) and `tests/e2e/trending.spec.ts` (expanded `SEEDED_THEMES` sample, renamed to reflect it's representative not exhaustive). Verified: Vitest 132/132, `tsc --noEmit` clean, full Playwright suite 71 passed/6 skipped (chromium/firefox/webkit/mobile-chrome), local dev seed run confirmed exactly 23 rows correctly sorted with no duplicates.

### This Session (2026-07-20)
- [x] **Item swap ("show me another") + per-bundle regenerate** — closes both Backlog P1 items from M2. Two new pure prompt-builders (`buildItemSwapPrompt`, `buildBundleRegeneratePrompt` in `src/lib/engine/prompt.ts`) and parsers (`parseItemResponse`, `parseSingleBundleResponse` in `src/lib/engine/parse-response.ts`), TDD'd (35 new Vitest tests). `callGemini` in `convex/generateBundles.ts` is now parameterized by response schema (previously hardcoded for the 3-bundle-array shape) so the new single-item/single-bundle Gemini calls can use their own schemas; a per-item media-enrichment helper (`enrichItemWithMedia`) was factored out of the existing per-bundle one for reuse. Two new Convex actions, `generateBundles:swapItem` and `generateBundles:regenerateBundle`, reuse the existing rate limiter (no new bucket) and patch the target bundle doc in place via two new `convex/bundles.ts` helpers (`getByIdInternal`, `patchItems`, `patchContent`) — Convex's reactivity means every page currently showing that bundle updates automatically. `BundleCard` gained a "🔄 Show me another" per-item button and a "🔄 Regenerate" per-bundle button, each with its own loading state, firing the already-reserved `item_swapped`/`bundle_regenerated` analytics events on success. No new auth/ownership checks (matches the existing no-ownership trust model on `bundles` docs) and no new rate-limit bucket, deliberately keeping scope tight per the plan's YAGNI calls (e.g. bundle-regenerate only avoids duplicating its *own* current theme, not sibling bundles' themes — full sibling-awareness would need prop-drilling across 3 call sites for a minor cosmetic benefit). **Verified end-to-end against local dev with real Gemini calls**: `swapItem` replaced a seeded item with a genuinely fitting, media-enriched replacement (real Unsplash image + photographer credit); `regenerateBundle` produced a distinctly different theme with 3 new items. Along the way, hit and correctly diagnosed a real external Gemini 503 "high demand" outage (confirmed via a direct `curl` bypassing Convex entirely) that also explained an earlier `quiz-flow.spec.ts` E2E flake this session — not a code regression, matching this project's own documented Gemini-flakiness gotcha. Extended `tests/e2e/auth-flow.spec.ts` with a regenerate step (one more real Gemini call, chromium-only). Full suite green: 71 passed, 6 skipped. Plan: `docs/superpowers/plans/2026-07-20-item-swap-bundle-regenerate.md`.
- [x] **M5 authenticated E2E test: signup → save → profile** (`tests/e2e/auth-flow.spec.ts`), closing a gap explicitly blocked on M4 until now. Uses `@clerk/testing`'s `clerk.signIn()` against a throwaway Clerk user created via the Backend API — deliberately not a real sign-up-form click-through, since Clerk's prebuilt `<SignUp>` is Clerk's own tested surface, not ours. Seeds its bundle via `testSupport:seedPopularBundle` rather than a real quiz completion, spending zero Gemini quota. Plan: `docs/superpowers/plans/2026-07-20-m5-authenticated-e2e.md`. Found and fixed several real issues while building it: (1) this Clerk instance has `password.used_for_first_factor: false` (only `email_code` is a first factor), so `clerk.signIn`'s password-strategy branch silently no-ops instead of throwing — switched to the email-based overload (creates a real backend sign-in token, uses the ticket strategy, works regardless of configured first factors); (2) discovered via the resulting "pending task" warning that this instance had Clerk **Organizations + org billing enabled** with `force_organization_selection: true` — unused scaffolding default forcing an org-selection step into every sign-in; disabled both (owner-approved) via `clerk disable billing --for orgs` + `clerk disable orgs`; (3) `testSupport:seedPopularBundle` now cleans up its own prior fixture rows first — repeated local runs had accumulated enough identically-scored "E2E Popular Bundle" entries to push new ones past `listPopular()`'s default 20-result cap; (4) a hard `page.goto()` reload (unlike a real user's client-side `<Link>` nav) can leave `SaveButton`'s `useAuth()` briefly behind the header's — its signed-out and signed-in-but-unsaved states render byte-identical markup, so a click can silently land on the stale guest button; added a `data-testid` to `BundleCard` for precise scoping, a defensive `try/catch` in `SaveButton`'s click handler, and a retrying `expect(...).toPass()` in the test; (5) fixed a bug in the test's own browser-scoping — `mobile-chrome` also runs on the Chromium engine, so checking `browserName` instead of `testInfo.project.name` didn't actually skip it, matching the existing convention in `quiz-flow.spec.ts`. Confirmed Google OAuth is already enabled (Clerk's shared dev credentials — no code change needed). Full suite verified green afterward: 71 passed, 6 skipped.
- [x] **Found + fixed a real bug: the `signup` webhook was silently failing in production the whole time.** Owner signed up a genuine test account and reported "no photos are shown now" (a red herring — see next bullet) while also asking to verify the webhook. Checked PostHog for a `signup` event: none existed at all, despite a real user having just been created (confirmed via `clerk users list --secret-key ... --order-by -created_at`). Pulled Vercel's function logs (`npx vercel logs perfectbundle.vercel.app`) and found the actual error: `Missing webhook signing secret. Set the CLERK_WEBHOOK_SIGNING_SECRET environment variable` — the secret existed in local `.env.local` (owner had added it after registering the Clerk Dashboard webhook) but was never pushed to Vercel's environment, so every delivery 400'd. Pushed it via stdin (`grep ... | npx vercel env add CLERK_WEBHOOK_SIGNING_SECRET production`, never appears in any command text or output) and redeployed. Verified end-to-end with a fresh throwaway test user (`clerk users create --secret-key ...`): Vercel logs showed a clean `info`-level request (no error), and PostHog's `event_definitions` showed a brand-new `signup` event definition with a `last_seen_at` matching the test to the second. Cleaned up the throwaway user afterward (left the owner's real test account alone).
- [x] **Diagnosed "no photos are shown now" as a false alarm — stale 24h generation cache, not a regression.** Ran a fresh (uncached) generation directly against prod: all 12 items got real images (8 eBay, 4 Unsplash), confirming the backend fix from earlier this session is fully intact. Cross-referenced the `bundles` and `generationCache` tables by creation time (`npx convex data <table> --prod --format json`) and found the story: a 08:24:43Z generation (before the env-var fix landed) has 0 images and is still cached under quiz hash `594bc9ae` (24h TTL, expires ~08:24 tomorrow); a 08:48:37Z generation with the *same shape* of quiz answers has full images. If the owner re-ran the exact same quiz answers as an earlier pre-fix attempt, they'd deterministically hit the stale cached (image-less) result — expected behavior given the cache design, not a new bug. No code change needed; resolves itself as old cache entries expire, or immediately if quiz answers differ even slightly.
- [x] **Confirmed phone-number sign-in is off** — owner disabled it themselves via the Clerk Dashboard. Verified via Clerk's public Frontend API (`GET https://<fapi-host>/v1/environment`, host derived by base64-decoding the publishable key's payload segment — no login needed since FAPI is unauthenticated): `phone_number.enabled: false`, only `email_address` + `password` remain active. No code change was needed or made — `<SignIn>`/`<SignUp>` are Clerk's prebuilt components and reflect instance-level settings dynamically.
- [x] **M3 Analytics closed out — real PostHog dashboard + funnel + attribution, built via API against real production data.** Owner generated a scoped personal API key (`insight:write`, `dashboard:write`, `event_definition:read` — PostHog's key UI is single-select per resource, None/Read/Write, not independent checkboxes, and Write already implies Read). Used it to first *prove* event delivery instead of asking the owner to eyeball PostHog's UI: `event_definitions` showed every M2/M3 event with today's `last_seen_at`, and a live funnel query returned genuine numbers (9 `$pageview` → 5 `quiz_started` → 4 `quiz_completed` → 3 `bundles_generated` → 2 `retailer_link_clicked`) — finally closing the "couldn't get automated confirmation" gap from earlier sessions. Then built "PerfectBundle Proof Dashboard" (pinned) with 12 insights covering every section of `docs/dashboard-spec.md` except Revenue (P2/manual by design): headline metrics via `TrendsQuery` formulas (e.g. `A/B*100` for quiz completion % and buy-intent CTR), core funnel (breakdown by `$device_type` and separately by `utm_source`), channel attribution (sessions+quiz-starts by UTM, plus a share-loop funnel filtered to `utm_source=share`), a 12-week trend line, engine health (cache-hit rate, generation failures), and retention events (wired and ready, 0 data until M4 events start firing). Dropped the spec's "p90 latency" tile — that property was never actually captured on `bundles_generated` (only `cache_hit`/`budget_band` are), so a filter for it would have been fabricated. Technique note: fetched PostHog's own auto-created default insights first to get real, authoritative `TrendsQuery`/`FunnelsQuery` JSON shapes instead of guessing the schema; also empirically confirmed the `@current` project-id shortcut works per-endpoint (failed against `/api/projects/@current/` needing `project:read`, but worked fine against `/insights/` and `/dashboards/` without it).
- [x] **eBay Partner Network campaign ID wired** (owner got approved and provided the campaign ID). Set `AFFILIATE_ID_EBAY` on Convex prod (server-side, feeds the Browse API affiliate header) and `NEXT_PUBLIC_AFFILIATE_ID_EBAY` on Vercel, then triggered a fresh production build — `NEXT_PUBLIC_` vars bake in at build time, so setting the var alone doesn't apply it to the already-running deployment. eBay buy-link clicks now earn commission.
- [x] **Root-caused and fixed: product photos weren't showing on the live site.** Investigation (`superpowers:systematic-debugging`): `git log` showed `origin/master` had been stuck at a 2026-07-18 commit — every M4 feature (auth, profiles, past-bundle memory, Popular tab) *and* the entire product-images/eBay pivot had only ever existed as local, unpushed commits. This session pushed all 36 commits to `origin/master` (owner-approved), which triggered a Vercel production deploy — but that only ships the Next.js frontend + Convex function *code*; it does **not** set Convex environment variables, which are deployment-scoped and separate from `.env.local`. `npx convex env list --prod --names-only` confirmed the production Convex deployment (`scintillating-cheetah-642`) only had `CLERK_JWT_ISSUER_DOMAIN` and `GEMINI_API_KEY` set — `EBAY_CLIENT_ID`, `EBAY_CLIENT_SECRET`, `UNSPLASH_ACCESS_KEY`, and `PEXELS_API_KEY` had never been pushed to prod, so every image-fetch path (eBay real-product photos *and* the Unsplash/Pexels representative-image fallback) silently no-opped per its best-effort design, leaving `item.imageUrl` undefined for every item — which `BundleCard` renders as no image block at all (not a broken-image icon, so this was easy to miss without checking the actual data). **This also means the "Verified live" claims in the 2026-07-18/19 checkpoint entries for images/eBay were checked against the local dev server (`npm run dev` + `npx convex dev`), not the real production deployment** — worth remembering: "live" in past entries wasn't always literally production. Fix: extracted the 4 missing keys from `.env.local` into a scratch file (values never echoed to chat, per the project's existing secret-handling convention) and ran `npx convex env set --from-file <file> --prod`, then `CONVEX_DEPLOYMENT=prod:scintillating-cheetah-642 npx convex deploy -y` to ensure the Convex function code itself was current on prod. Verified with a real, uncached generation run directly against prod via `npx convex run generateBundles:generate '{...}' --prod`: 10/12 items got genuine eBay photos + prices + direct buy links, the other 2 correctly fell back to credited Unsplash representative images — exactly the designed layering, now actually live. Confirmed via `npx vercel inspect perfectbundle.vercel.app` that the production alias also points at this session's just-built deployment (frontend and Convex backend are now both current).
- [x] **Fixed blurry/oversized product photos.** eBay's Browse API returns a 225px default thumbnail; it was being stretched across the full card width, which is what caused the blur. Added `upscaleEbayImageUrl` (`src/lib/engine/media.ts`) to swap eBay's CDN size token for `s-l500` (verified via direct `curl` that eBay's CDN actually serves distinct larger files at that path, not just the same 225px image relabeled), and changed `BundleCard`'s item image from a full-width `h-40` banner to a `h-24 w-24` thumbnail next to the item text. TDD'd (3 new `media.test.ts` cases). Deployed to both Convex prod and Vercel. Note: only new generations get the fix — anything already in the 24h generation cache or already saved/shared keeps its old image until regenerated.
- [x] **`signup` PostHog event wired via a Clerk `user.created` webhook** — `src/app/api/webhooks/clerk/route.ts` (the project's first Next.js Route Handler). Chose a server-side webhook over a client-side "just signed up" heuristic deliberately: PerfectBundle has three signup entry points (header `SignUpButton` modal, the save-action guest upsell modal, the dedicated `/sign-up` page), and Clerk only fires `user.created` once per account regardless of which one was used — so the webhook gives exactly-once semantics for free, with no localStorage/dedup logic needed on our side. Verifies the webhook signature with `@clerk/nextjs/webhooks`' `verifyWebhook` (no new dependency — it transitively uses `standardwebhooks`, already installed via `@clerk/nextjs`, not `svix`). Added `src/lib/analytics-server.ts` (`captureServerEvent`) as the server-side counterpart to `src/lib/analytics.ts`'s `track()`: `posthog-js` is browser-only, so this POSTs directly to PostHog's capture API (`{host}/i/v0/e/`) instead — same env vars/host fallback as `instrumentation-client.ts`, best-effort (swallows fetch errors so a PostHog outage can never fail the webhook ack to Clerk). Added `src/lib/webhooks/clerk-signup.ts` (`signupMethodFromExternalAccounts`) to map Clerk's `externalAccounts` shape to the PRD's `method` property (`"email"` when none, else the OAuth provider name with Clerk's `oauth_` prefix stripped). **Deliberately scoped out**: the PRD's `signup` event also lists a `bundle_id` property, but a Clerk webhook payload carries no UI-flow context to attach one — omitted rather than faked. Plan: `docs/superpowers/plans/2026-07-20-signup-event-clerk-webhook.md`. Verified: Vitest 111/111, `tsc --noEmit` clean, `next build` clean (new route appears at `/api/webhooks/clerk`). **Blocked on owner** for live verification: register the webhook in Clerk Dashboard (Webhooks → Add Endpoint → `https://perfectbundle.vercel.app/api/webhooks/clerk` → subscribe to `user.created`) and set `CLERK_WEBHOOK_SIGNING_SECRET` in Vercel from the endpoint's Signing Secret — see `docs/handover.md` §2/§4.

### This Session (2026-07-19)
- [x] **Real eBay product data live** — owner got approved on eBay + Amazon (Etsy rejected, now fully removed from the codebase with a regression test). Wired eBay's Browse API as the primary real-product layer: OAuth2 client-credentials token (`getEbayToken`, one fetch per generation, not per item — `btoa`-based Basic auth since Convex's default runtime has no Node `Buffer`), per-item search (`fetchEbayProduct`), pure response parsing (`parseEbayItemSummary`, `ebayMarketplaceForCountry`, `formatEbayPrice` in `src/lib/engine/media.ts`, TDD'd). `chooseItemMedia`'s `sovrn` parameter generalized to `realProduct` since eBay — not Sovrn — is the live provider (Sovrn stays as a documented, unused alternate behind the same seam). **Verified live** with a real uncached generation: every item across all 3 bundles got a genuine eBay photo, direct buyable item URL, and real price.
- [x] **Dual stock-image providers** — switched Phase 1 images from Pexels-only to **Unsplash primary + Pexels fallback** (more free-tier headroom), and added required photographer attribution ("Photo by X on Unsplash/Pexels") per Unsplash's API terms. Verified live: both providers actively serving real, correctly-credited images.
- [x] **Found and fixed a real pre-existing bug**: Amazon/eBay affiliate tags never reached outbound links because `src/lib/links/retailer-links.ts` (called from the `"use client"` `BundleCard`) read plain `AFFILIATE_TAG_AMAZON`/`AFFILIATE_ID_EBAY` — Next.js only inlines `NEXT_PUBLIC_`-prefixed vars into the browser bundle, so the tag was always `undefined` client-side regardless of what was set in Vercel. Renamed both, added a regression test, documented the client/server env-var split (a separate server-only `AFFILIATE_ID_EBAY` now also feeds the eBay Browse API's affiliate header).
- [x] Etsy fully removed from `src/lib/links/retailer-links.ts` (app rejected) — regression test asserts no Etsy link/URL can silently reappear.
- Plan: `docs/superpowers/plans/2026-07-19-ebay-real-products-affiliate-fix.md`. Verified: Vitest 100/100, `tsc --noEmit` clean, `next build` clean, Playwright chromium 18/18.

### This Session (2026-07-18)
- [x] Product-data strategy pivot + Phase 1 images: Etsy rejected the app (Amazon/eBay likely too), so instead of per-retailer approval we go through **Sovrn Commerce** — a free, day-one-approval aggregator whose Product API returns real photo + direct affiliate buy link + price + merchant across all major retailers. Brainstormed + spec'd (`docs/superpowers/specs/2026-07-18-product-data-and-images-design.md`). **Phase 1 shipped**: representative item images via free Pexels API (env-gated `PEXELS_API_KEY`, fetched+cached at generation, "Representative image" caption, best-effort/never-blocks), pure unit-tested `src/lib/engine/media.ts` with first-match-wins layering (Sovrn→stock→none) so Phase 2 is a drop-in. Plan: `docs/superpowers/plans/2026-07-18-phase1-representative-images.md`. Verified: Vitest 89/89, tsc/build clean, Playwright chromium 16/16 (new `bundle-images.spec`). Phase 2 pending owner's Sovrn key.
- [x] Phase 2 UI + key diagnosis: built the shape-independent half of Phase 2 — a primary "Buy at {merchant}" affiliate button, real price, and an FTC affiliate disclosure that appears whenever an item carries `productUrl` (`src/components/bundles/bundle-card.tsx`, `tests/e2e/affiliate-buy.spec.ts`). **Blocked findings (owner keys):** (1) the Pexels key in `.env.local` is invalid — Pexels returns "Invalid API key" for every fresh query (a cached "coffee" query masked it); it's 32 chars vs Pexels' ~56, so a wrong value was pasted. Phase 1 code is proven correct against a valid response. (2) Sovrn Product API returns 401 "Invalid Api Key" (campaign likely not yet approved; schema is unpublished), so the Sovrn fetch/parser can't be built until one authenticated call succeeds. Env var names corrected to `SOVRN_SITE_API_KEY` + `SOVRN_SECRET_KEY` (two values). Verified this increment: Vitest 89/89, tsc/build clean, Playwright chromium 17/17.
- [x] M3/M4 engagement counters + Popular tab: `convex/engagement.ts` `record` upserts per-bundle counters at every click/save/share/view touchpoint (curated + generated); pure unit-tested `popularityScore` (`src/lib/bundles/popularity.ts`); `convex/engagement.ts` `listPopular` ranks publicly-shared user-generated bundles; new `/popular` page (distinct from editorial `/trending`, cross-linked, cold-start empty state). Plan: `docs/superpowers/plans/2026-07-18-m4-popular-tab.md`. Verified: Vitest 82/82, tsc + build clean, Playwright chromium 15/15 (incl. new `popular.spec`). Closes the M3 P1 "engagementCounters wired" task and the M4 Popular tab.
- [x] M4 past-bundle memory: `recipientProfiles.pastItemNames` schema field + `getByIdInternal`/`appendPastItemsInternal` (cap 50, deduped); `buildBundlePrompt` gains an optional `pastItemNames` param → "avoid repeating" prompt line + rule; `QuizState.profileId` threaded through prefill → submit → results (deliberately outside `QuizAnswers` so the cache hash/stored quiz shape are unchanged for the no-profile path); `generateBundles.generate` verifies profile ownership server-side, excludes past items, folds `profileId` into the cache key, and appends new item names after a fresh generation. Plan: `docs/superpowers/plans/2026-07-18-m4-past-bundle-memory.md`. Verified: full Vitest suite 79/79 green, `tsc --noEmit` clean, `next build` clean, Playwright chromium 13/13 (incl. real-Gemini quiz→results completion).

### This Session (2026-07-17)
- [x] Brainstorming: design approved → `docs/superpowers/specs/2026-07-17-perfectbundle-design.md`
- [x] PRD written → `docs/prd.md`
- [x] Scaffold docs: `docs/tasks.md`, `docs/planning.md`, `docs/checkpoint.md`, `CLAUDE.md`, `docs/data-schema.md`, `docs/dashboard-spec.md`
- [x] Git repo initialized
- [x] M1 plan written → `docs/superpowers/plans/2026-07-17-m1-foundation.md`
- [x] Next.js 16 scaffold (App Router, TS, Tailwind) — build green
- [x] Vitest infra; Zod bundle schemas + 5 seed bundles (TDD, 6/6 tests)
- [x] Convex: schema pushed to local dev deployment, seed idempotent, `curated:listApproved` verified
- [x] Env-gated Convex provider + PostHog init + typed `track()` (builds with zero env vars)
- [x] Landing page shell + `/quiz` placeholder (Framer Motion)
- [x] GitHub Actions CI; pushed to github.com/alanhasbiro/perfectbundle
- [x] Convex agent skills installed (`.claude/skills/convex-*`)
- [x] M2 quiz wizard: pure state machine, country detection, wizard shell, 6 steps, routing (23 tests)
- [x] M2 link builder: Amazon/Etsy/eBay URL builder, 16 countries, affiliate slots, fast-shipping hint (TDD)
- [x] M2 bundle engine: prompt builder, response parser, quiz hashing, Convex `generate` action (Gemini + cache + rate limit + retry), golden-fixture suite — verified live with a real Gemini call
- [x] M2 results UI + share + trending: `<BundleCard>`, results page wired to the engine with trending fallback, `/b/[id]` share page (Server Component, OG tags), `/trending` browse page — full journey live-verified including the failure-fallback path
- [x] **Production Convex deployment**: `npx convex deploy` → `scintillating-cheetah-642.convex.cloud`, Gemini key set, curated bundles seeded (owner completed `npx convex login`)
- [x] M5 sprint 1: Playwright E2E installed + configured (auto-starts Convex + Next dev servers, no manual setup); 8 passing specs (landing, quiz back-nav, one full quiz→results completion, trending, share incl. not-found case)
- [x] Real bug caught and fixed by E2E testing: `submit()` calling `router.push()` inside a `setState` updater (React warning) — fixed in `src/components/quiz/use-quiz.ts`
- [x] SEO basics: `src/app/robots.ts`, `src/app/sitemap.ts`, landing page OG metadata + `metadataBase`
- [x] M5 sprint 2: cross-browser (firefox, webkit) + mobile viewport (Pixel 7) Playwright projects; the one real-Gemini test capped to chromium only via `test.skip`
- [x] Accessibility audit (axe-core): landing, quiz, trending — found and fixed a real WCAG contrast issue (headline fade-in transient low-contrast state); added site-wide `prefers-reduced-motion` support (`src/components/motion-config-provider.tsx`)
- [x] Convex AI guideline files updated (`npx convex ai-files update`)

---

## In Progress 🔄

### Current Focus
> **Milestone 2 P0 scope is done and Milestone 5 has a working core E2E suite.** Full user journey works end-to-end, live-verified against BOTH local dev and now a real production Convex deployment: landing → quiz (6 steps) → "Building your bundles…" → 3 real Gemini-generated themed bundles with working Amazon/Etsy/eBay buy links → Share button copies a working `/b/<id>` public page (Server Component, real OG tags) → `/trending` browse page works standalone with the 5 seeded curated bundles. Generation failure/rate-limit falls back to trending bundles inline, never a dead end (tested live by disabling the Gemini key). All P0 analytics events fire.
>
> Production Convex is now live at `scintillating-cheetah-642.convex.cloud` (Gemini key set, curated bundles seeded). **Owner still needs to** add `NEXT_PUBLIC_CONVEX_URL=https://scintillating-cheetah-642.convex.cloud` to Vercel's Production environment variables — the next push will then deploy a working production site (previously the live site was pointed at a local-only Convex backend Vercel could never reach).
>
> Playwright E2E suite (`npm run test:e2e`) is green across chromium, firefox, webkit, and mobile-chrome: 41 passed, 3 skipped (the one real-Gemini test intentionally runs on chromium only to conserve free-tier quota). Self-contained — auto-starts both dev servers, no manual setup. Also added a baseline accessibility audit (axe-core) on the three static-ish pages; it caught a real WCAG contrast issue in the landing headline's fade-in animation, which is now fixed with proper site-wide `prefers-reduced-motion` support. SEO basics (robots.txt, sitemap.xml, OG metadata) also done. Caught and fixed two real bugs today via this testing work (see Completed Items).
>
> Deferred to backlog (P1, needs new engine capability, not blocking launch): single-item swap and per-bundle regenerate. Only remaining M5 item: Lighthouse performance scoring (separate tooling, lower priority).
>
> **Both PostHog and Clerk account creation instructions were given to the owner this session** — M3 (PostHog dashboard) and M4 (Clerk accounts/saved bundles/profiles/reminders) are ready to build the moment those keys land in `.env.local`. Everything else buildable without those keys is now done.

---

## Blockers 🚫

| Blocker | Impact | Status | Resolution |
|---------|--------|--------|------------|
| — none — | | | |

**External dependencies to set up when M1 starts (all free):** Gemini API key, Convex account, Vercel account, PostHog account, GitHub repo.

---

## Next Actions 📋

### Immediate (Next Commit)
1. [ ] **Owner:** visit perfectbundle.vercel.app, then check PostHog → Activity → Events to confirm events are actually landing (couldn't get automated proof — see M3 row above)
2. [ ] Build M3 dashboard/funnel/attribution insights in PostHog UI (manual, ~10 min, no API access available to automate)
3. [ ] **Owner:** try signing up as the first test user at perfectbundle.vercel.app to confirm the Clerk flow works end to end
4. [ ] Build M4: save bundle action (guest→signup upsell), "My bundles" page, recipient profiles CRUD, occasion reminders
5. [ ] When ready to leave Clerk dev-mode: create a production instance in the Clerk dashboard (needs a verified custom domain) and re-run `clerk env pull --instance prod`

### Short-term
- [ ] Lighthouse performance pass (M5, last remaining item, needs separate tooling setup)
- [ ] Wire E2E suite into GitHub Actions CI once a CI-safe Gemini key/quota strategy is decided
- [ ] Convex `engagementCounters` (M3 P1, clicks/saves/shares per bundle) — not started

---

## Scope Changes 📝

| Date | Change | Reason | Impact |
|------|--------|--------|--------|
| — | | | |

---

## Technical Decisions Log 🔧

| Date | Decision | Rationale | Alternatives Considered |
|------|----------|-----------|------------------------|
| 2026-07-17 | AI-curated engine + retailer search deep-links (no retailer APIs in MVP) | Only truly $0 + global approach | Retailer APIs first (US-skewed, approval friction); hand-curated catalog (unsustainable) |
| 2026-07-17 | Convex + Next.js/Vercel + Clerk + Resend + PostHog + Gemini free tiers | $0 hard constraint | Supabase (viable; Convex chosen for cron/actions/realtime + mobile reuse) |
| 2026-07-17 | Guest-first, auth deferred to M4 | Conversion + smaller MVP | Auth-from-day-1 |
| 2026-07-17 | In-app checkout/dropshipping parked to Phase 4+ | Payments/refunds/tax overhead unjustified pre-traffic | Building marketplace first |
| 2026-07-17 | Gemini model: `gemini-flash-latest` (resolves to gemini-3.5-flash) | `gemini-2.5-flash`/`-lite` returned 404 "no longer available to new users" on live test; `-latest` alias self-updates, avoiding future breakage | Pinning an explicit version (more predictable but needs manual updates as Google deprecates models) |
| 2026-07-17 | E2E share-page test seeds data via a dedicated `convex/testSupport.ts` mutation, not a full quiz→share click-through | Zero Gemini cost, fast, deterministic; a live click-through already exercises share via quiz-flow.spec.ts indirectly | Mocking the Convex action at the network layer (more setup, less confidence in real integration) |
| 2026-07-17 | Playwright E2E runs serially (`workers: 1`), not wired into CI yet | Suite shares one local Convex backend + rate limiter; CI would need a dedicated Gemini quota strategy not yet decided | Parallel workers (tried first, worked by luck — not relied upon) |
| 2026-07-17 | PostHog project confirmed on EU cloud (`eu.i.posthog.com`/`eu-assets.i.posthog.com`), not US | Direct API test against `app.posthog.com` (legacy US alias) returned 401/404; switching host to `eu.i.posthog.com` fixed config load | Assumed US region by default (wrong) |
| 2026-07-17 | Kept `instrumentation-client.ts` as the single PostHog init site; did not add a second React-provider-based init | A duplicate `posthog.init()` call triggers the SDK's own "already initialized" guard and silently drops the second call's config — found by testing, not inspection | A `PostHogProvider` React component (built, then reverted once the conflict was found) |
| 2026-07-17 | Do not use `setTimeout`/`new Promise(resolve => setTimeout(...))` for delays inside a Convex action | A bare `setTimeout`-based delay in `generateBundles:generate`'s retry logic never resolved — the action hung indefinitely, and because it never returned, it occupied the deployment's action concurrency slot and blocked *every other* action invocation (CLI and browser) until Convex's own platform timeout eventually force-killed it minutes later. Caused a real incident: users saw "Building your bundles…" stuck forever instead of the curated fallback. Reverted immediately; if a retry backoff is wanted later, use `ctx.scheduler.runAfter` or verify Convex's supported delay mechanism first, don't assume Node-style timers work | An explicit backoff delay before the Gemini retry (reverted — do not reintroduce without verifying the delay mechanism against Convex's actual runtime) |

---

## Questions & Clarifications ❓

### Open Questions
- [ ] App display name/domain: "PerfectBundle" assumed from folder name — confirm before launch
- [ ] Which social channels for launch posts (affects UTM plan)

### Resolved
- [x] Fulfillment model → links out + ideas; middleman checkout parked (user, 2026-07-16)
- [x] Market → global, region-adaptive links (user, 2026-07-16)
- [x] MVP features → quiz+bundles, save/share, profiles, reminders, trending+popular (user, 2026-07-16)
- [x] Engine approach → A + curated trending (user, 2026-07-16)

---

## Change Log 📜

| Date | Commit | Summary |
|------|--------|---------|
| 2026-07-17 | 8f6a935 | Design spec from brainstorming |
| 2026-07-17 | be63c3e | PRD + full docs scaffold + CLAUDE.md |
| 2026-07-17 | e1a247b | M1 Foundation plan |
| 2026-07-17 | 2fbed75…43fc17e | M1 execution: scaffold, Vitest, schemas+seed (TDD), Convex, providers, PostHog, landing, CI |
| 2026-07-17 | 3a90482…0c485fa | Convex agent skills + M1 docs closeout |
| 2026-07-17 | 34551a9 | M2 quiz wizard sprint plan |
| 2026-07-17 | 97170ff…d1f1e30 | M2 quiz wizard: state machine, country, shell, 6 steps, routing (TDD, 23 tests) |
| 2026-07-17 | 7e52b79 | M2 quiz wizard docs closeout |
| 2026-07-17 | 3ea259b | M2 link builder + bundle engine sprint plan |
| 2026-07-17 | 9f9aff7…f0e378e | M2 link builder + bundle engine: retailer links, prompt, parser, golden fixtures, hash, Convex generate action (59 tests, verified live) |
| 2026-07-17 | 304a4c5 | M2 engine docs closeout |
| 2026-07-17 | efcf6db | M2 results UI + share + trending sprint plan |
| 2026-07-17 | cf7fd63…47ed01c | M2 results UI + share + trending: session-id, budget-status, Convex share fns, BundleCard, results wiring, /b/[id], /trending (71 tests, live-verified incl. failure fallback) |
| 2026-07-17 | 641d401 | M2 docs closeout — Milestone 2 P0 scope complete |
| 2026-07-17 | e27e2a7 | M5 Playwright E2E sprint plan |
| 2026-07-17 | — (CLI) | Production Convex deployed (`scintillating-cheetah-642.convex.cloud`), Gemini key set, curated bundles seeded |
| 2026-07-17 | 0961590…e49269a | M5 sprint 1: Playwright install/config, testSupport seed mutation, 4 spec files (8 tests), setState-in-render bug fix, serial-workers reliability fix |
| 2026-07-17 | 618c736 | M5 sprint 1 docs closeout |
| 2026-07-17 | 8a9cf71 | SEO basics: robots.txt, sitemap.xml, landing OG metadata |
| 2026-07-17 | eaac2ea | M5 sprint 2 (cross-browser, mobile, a11y) plan |
| 2026-07-17 | 56d5b2d…ec6c850 | M5 sprint 2: firefox/webkit/mobile-chrome projects, axe-core a11y audit + real reduced-motion fix, Convex AI files updated |
| 2026-07-17 | 8c7125d, 226afd2 | M3: fixed PostHog region mismatch (EU not US) in Vercel prod env vars; found+reverted a redundant double-init; confirmed live prod config resolves. Automated event-delivery proof still unresolved — flagged for manual PostHog UI check |
| 2026-07-17 | f04b216 | M4: Clerk auth foundation — ClerkProvider, sign-in/sign-up pages, site header, guest-first middleware fix (reverted clerk init's default protect-everything scaffold) |
| 2026-07-17 | 814e03c | Redeploy for Clerk production env vars (dev-mode keys, no prod instance yet) |
| 2026-07-17 | 1723ffd | M4: save bundles + /my-bundles — Convex↔Clerk auth wired (auth.config, ConvexProviderWithClerk, "convex" JWT template). NOTE: manually-created Clerk JWT templates omit `aud` by default; had to PATCH `aud:"convex"` in or Convex rejects the token (aud mismatch) — the dashboard "Convex" preset sets this automatically, a hand-made template does not |
| 2026-07-17 | b784591 | M4 docs: mark save-bundles + My-bundles complete |
| 2026-07-17 | 872c1b7 | docs: monetization strategy (affiliate-first, phased, $0-to-run) → `docs/monetization.md` |
| 2026-07-17 | ed44b7b | M4: recipient profiles CRUD + "new bundles for X" prefill (`convex/recipientProfiles.ts`, `/profiles`, `src/lib/quiz/prefill.ts`, `src/lib/quiz/options.ts`) |
| 2026-07-17 | e57bb2e | docs: add `handover.md` single-entry-point session handoff |
| 2026-07-18 | 074c849…5da29cb | M4 past-bundle memory: pastItemNames schema + internal lookups, prompt avoid-repeating instruction, profileId threaded through quiz state (outside QuizAnswers), generateBundles ownership-verified dedup + profile-folded cache key |
| 2026-07-18 | 3e8aed3 | docs closeout — M4 past-bundle memory complete |
| 2026-07-18 | ae41e9d…060896b | M3/M4: engagementCounters wired + Popular tab (`convex/engagement.ts`, `src/lib/bundles/popularity.ts`, `/popular`, popular.spec E2E) |
| 2026-07-18 | 9925370 | docs closeout — engagement counters + Popular tab complete |
| 2026-07-18 | 308e4c1, b53b546 | Sovrn product-data workaround: design spec + Phase 1 plan |
| 2026-07-18 | 3a1dd8c…96c28a5 | Phase 1 representative images: media fields, pure media module, env-gated Pexels enrichment, BundleCard image + caption |
| 2026-07-18 | 3a1dd8c…01d7e2d | docs closeout — Phase 1 representative images complete |
| 2026-07-18 | d92244d | Phase 2 UI: Buy-at-merchant affiliate button + disclosure (shape-independent; Sovrn parser still blocked on approved campaign) |
| 2026-07-18 | f735f65 | docs: Phase 2 UI done; Pexels/Sovrn keys blocked (invalid key / unapproved campaign) |
| 2026-07-19 | 413b5d1 | Etsy removed; Unsplash primary + Pexels fallback with photographer attribution |
| 2026-07-19 | 7ad56bf | docs: eBay+Amazon approved, Etsy removed; Unsplash+Pexels image providers |
| 2026-07-19 | f362aa3 | eBay real-products + affiliate-tag bug fix implementation plan |
| 2026-07-19 | ddca52d | fix: Amazon/eBay affiliate tags need NEXT_PUBLIC_ prefix to reach the browser |
| 2026-07-19 | 28723b1 | eBay parsing/marketplace/price logic; chooseItemMedia generalized to realProduct |
| 2026-07-19 | 2e373b1 | eBay Browse API wired as the primary real-product layer (OAuth + per-item search) |
| 2026-07-19 | pending | docs closeout — eBay real products + affiliate-tag fix complete |
| 2026-07-23 | ee486e7 | Warm & bold visual redesign: design spec |
| 2026-07-23 | a46494b | Warm & bold visual redesign: implementation plan |
| 2026-07-23 | 4225648 | Palette tokens, `.btn-primary`/`.btn-secondary`/`.card-surface` component classes, contrast test |
| 2026-07-23 | fbd4876 | Warm styling: landing page + header |
| 2026-07-23 | b459b0b | Warm styling: quiz wizard, chips, urgency step |
| 2026-07-23 | 407e2cd | Warm styling: BundleCard + SaveButton (propagates to results/trending/popular/share/my-bundles) |
| 2026-07-23 | 1a24296 | Warm styling: quiz results empty state |
| 2026-07-23 | 1d42504 | Warm styling: share, profiles, my-bundles pages |
| 2026-07-23 | 84d7c98, 88718bd | Warm styling: Clerk auth UI (`<SignIn>`/`<SignUp>`/`<UserButton>`) |
| 2026-07-23 | e00827d | fix: Clerk v7 `appearance.variables` key names (build-blocking regression found in Task 8 verification, attempt 1) |
| 2026-07-23 | f79afc3 | docs: fix stale comment terminology after Clerk variable key rename |
| 2026-07-23 | 8845ec2 | fix(a11y): `opacity-60`→`opacity-70` warm-palette contrast regression (found in Task 8 verification, attempt 2) + new regression test |
| 2026-07-23 | 1cfc3b4 | docs: warm & bold visual redesign completion (checkpoint + handover) |
| 2026-07-23 | 2083e77 | fix: apply warm styling to `ProfileForm` — missed by every task's file list (found in final whole-branch review), still had the old flat-black submit button on `/profiles`' create/edit flows |

---

## Notes 📌

### Handoff Notes
Start any session by reading the 4 source-of-truth docs (`docs/prd.md`, `docs/tasks.md`, `docs/planning.md`, this file). M1 and M2's P0 scope are done and live-verified (both locally and against a real production Convex deployment); M5 has a green core E2E suite. Two external owner actions are pending and block further progress on M3/M4 specifically (not the app in general): a PostHog key and Clerk keys — see Next Actions above. Hard constraint to preserve in every decision: **$0 operating cost** (see `docs/planning.md` §3 cost policy).

Local dev now needs `npx convex dev` running (not just `--once`) if you want the Next.js dev server or Playwright E2E suite to actually reach the Convex backend — Playwright's config auto-starts it; manual `npm run dev` sessions need it started separately in another terminal (or rely on Playwright's webServer for E2E work instead).

*Next checkpoint: before next commit*
